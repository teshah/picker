# picker

# Project Setup

To build and run the project, follow these steps:

1. Build the project using Vite:
   ```sh
   npx vite build
   ```

2. Run it
   ```sh
   npm run dev
   or
   pnpm run dev
   ```

